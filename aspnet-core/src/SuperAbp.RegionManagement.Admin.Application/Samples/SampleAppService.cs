using System.Threading.Tasks;
using Microsoft.AspNetCore.Authorization;

namespace SuperAbp.RegionManagement.Samples;

public class SampleAppService : RegionManagementAppService, ISampleAppService
{
    public Task<SampleDto> GetAsync()
    {
        return Task.FromResult(
            new SampleDto
            {
                Value = 42
            }
        );
    }

    [Authorize]
    public Task<SampleDto> GetAuthorizedAsync()
    {
        return Task.FromResult(
            new SampleDto
            {
                Value = 42
            }
        );
    }
}
